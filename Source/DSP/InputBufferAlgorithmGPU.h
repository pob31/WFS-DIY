#pragma once

#if GPU_AUDIO_ENABLED

#include "GpuInputBufferAlgorithm.h"

// Compatibility shim to match older project file naming.
using InputBufferAlgorithmGPU = GpuInputBufferAlgorithm;

#endif // GPU_AUDIO_ENABLED
